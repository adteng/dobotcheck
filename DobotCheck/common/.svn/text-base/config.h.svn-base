#ifndef CONFIG_H
#define CONFIG_H

#include <windows.h>
//#include <winsock2.h> //already define in windows.h
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <process.h>
#include <conio.h>
#include <winbase.h>

#include <time.h>
#include <list>
#include <vector>
#include <algorithm>
#include <math.h>
#include <fstream>
#include <iostream>
#include "des.h"

/*
#ifndef _NOSTD
	using std::copy;
	using std::equal_range;
	using std::back_insert_iterator;
	using std::ostream_iterator;
	using std::pair;
	using std::min_element;
	using std::string;
	using std::list;
	using std::vector;
	using std::set;
	using std::find;
	using std::find_if;
	using std::binary_search;
	using std::lower_bound;
	using std::upper_bound;
#endif
*/
#endif